release-pr:
  stage: sisyphus
  image: oven/bun:latest
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      changes:
        - .sisyphus/stones/**/*
  script:
    - bun install
    - bunx @r5n/sisyphus actions release-pr
